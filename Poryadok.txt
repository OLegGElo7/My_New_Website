1))))))))))))) - создание двух и более коммитов в одной ветке



cd ~/Desktop(название рабочего стола)  --- зайти на рабочий стол

cd My-New-Website(название папки) --- зайти в папку проекта 

``ls --- проверить содержимое

git init --- создание первой ветки

git add . --- подготовка для первого коммита

``git status --- проверить статус файлов для подготовки

git commit -m "Название коммита" ---- создание первого коммита

``git log --- посмотреть наличие коммитов

rm -r(rm - удаление файла, rm -r -удаление каталога) --- удаляем содержимое для подготовки к второму коммиту

``git status --- проверить статус файлов 

``перекидываем файлы в папку

``ls --- проверяем наличие файлов в папке

git add . --- подготовка для второго коммита

`` git status --- проверяем готовность файлов 

git commit -m "название второго коммита" --- создали второй коммит

`` git log ---- смотрим наличие коммитов

git checkout  53b7bd0(номер первого коммита) --- head перетаскиваем на первый коммит

``ls --- смотрим наличие первого коммита

cd Website_one(название папки в первом коммите) --- заходим в первую папку

cd ..  ---- выходим из папки

git checkout master --- head возвращаем в актуальный коммит

git branch -m main(название) --- переименовываем ветку 

git branch ---- смотрим наличие веток

2))))))))))) - создание второй ветки

git branch -b new-feature --- создание второй ветки и сразу переход head в нее

code . ---- переход в вижуал студия код


`` перетаскиваем файлы в папку

``ls

git add .

git commit -m "Название коммита"

`` git log












